import { UixLightNotifierDirective } from './uix-light-notifier.directive';

describe('UixLightNotifierDirective', () => {
  it('should create an instance', () => {
    const directive = new UixLightNotifierDirective();
    expect(directive).toBeTruthy();
  });
});
